gastos_publicos:
  outputs:
    dev:
      dbname: airflow
      host: postgres_estudos
      pass: airflow
      port: 5432
      schema: public
      threads: 4
      type: postgres
      user: airflow
  target: dev
